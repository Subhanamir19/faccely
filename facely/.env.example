# Copy to .env and adjust for your deployment target.
# Production builds default to the deployed Railway API. Override only when needed.
EXPO_PUBLIC_API_BASE_URL=https://faccely-production.up.railway.app

# Google OAuth client IDs (configure in Google Cloud console):
# - EXPO_PUBLIC_GOOGLE_CLIENT_ID powers Expo Go / web auth and must allow the redirect URI
#   https://auth.expo.io/@subhan_amir/sigmamax.
# - Native apps additionally require platform-specific IDs that allow the custom scheme redirect
#   sigmamax:/oauthredirect (plus your package/bundle identifiers).
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_CLIENT_ID=

# For production, point to your deployed scorer API, e.g.:
# EXPO_PUBLIC_API_URL=https://api.your-domain.com
