OPENAI_API_KEY=sk-rotate-me-later
PORT=8080
CORS_ORIGINS=*
RATE_LIMIT_PER_MIN=30

# ML Scoring API (replaces OpenAI for scoring, keeps OpenAI for explanations)
ML_SCORING_API_URL=http://localhost:8000
ML_SCORING_ENABLED=true

# Supabase (required)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Supabase JWT verification (required after Clerk removal)
SUPABASE_JWT_SECRET=
SUPABASE_ISSUER=
SUPABASE_AUDIENCE=authenticated
